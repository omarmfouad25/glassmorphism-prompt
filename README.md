![liquid-glass-ui preview](./screenshot.jpeg)

# liquid-glass-ui

**Copy one prompt. Get this hero section.**

Three.js video background. 3-tier glass CSS. XOR gradient borders. Micro-interactions. Full Next.js implementation — generated by AI in one shot.

Works with Claude, GPT-4o, Gemini, or any capable LLM.

---

## The Prompt

Fill in your brief below, then copy the entire block and paste it into your AI of choice.

### Your Brief

Replace every `[ALL CAPS]` placeholder before copying.

| Placeholder | What to put here | Example |
|-------------|-----------------|---------|
| `[BRAND]` | Product or brand name | Bloom |
| `[HEADING]` | Full h1 text | Build something beautiful |
| `[ACCENT_WORDS]` | Which word(s) in the heading get serif italic | beautiful |
| `[TAGLINE]` | Short line below the heading | The future, beautifully made. |
| `[CTA]` | Primary button text | Get Started |
| `[PILLS]` | 2–4 short tag labels | Design, Motion, Code |
| `[VIDEO_URL]` | Background .mp4 or image URL | /hero.mp4 |
| `[QUOTE]` | Optional footer quote + attribution (remove line if none) | "Design is intelligence made visible." — Alina Wheeler |
| `[CARDS]` | 2–3 right panel cards: title + one-line description each | Analytics: Real-time data at a glance |

---

### Copy this ↓

```
Build a premium glassmorphism hero section for a Next.js project.

BRIEF
  Brand:         [BRAND]
  Heading:       [HEADING]
  Accent words:  [ACCENT_WORDS] — wrap these in Source Serif 4 italic
  Tagline:       [TAGLINE]
  CTA button:    [CTA]
  Tag pills:     [PILLS]
  Background:    [VIDEO_URL]
  Footer quote:  [QUOTE]  ← remove this line if no quote
  Right panel cards:
    - [CARD 1 TITLE]: [CARD 1 DESCRIPTION]
    - [CARD 2 TITLE]: [CARD 2 DESCRIPTION]
    - [CARD 3 TITLE]: [CARD 3 DESCRIPTION]  ← optional third card

Stack: Next.js 14+ App Router, Tailwind CSS 3+, @react-three/fiber, @react-three/drei,
lucide-react. Before writing files, check if Three.js is installed. If not:
  npm install three @react-three/fiber @react-three/drei && npm install -D @types/three

Follow these rules exactly. Do not simplify or deviate from the specified values.

─────────────────────────────────────────────────────────────────────────────
RULE 1 — 3-TIER GLASS CSS
─────────────────────────────────────────────────────────────────────────────

Inject under @layer components in globals.css. Do not alter any values.
Three tiers only — never invent new glass values.

  .glass-panel  →  heavy   (60px blur) — page overlays, feature sections
  .glass-card   →  medium  (30px blur) — cards nested inside panels
  .glass-pill   →  light   (16px blur) — buttons, tags, nav items

@layer components {

  .glass-panel, .glass-card, .glass-pill {
    position: relative;
    overflow: hidden;
  }

  .glass-panel::before,
  .glass-card::before,
  .glass-pill::before {
    content: '';
    position: absolute;
    inset: 0;
    padding: 1px;
    border-radius: inherit;
    pointer-events: none;
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
  }

  .glass-panel {
    background: rgba(0,0,0,0.35);
    backdrop-filter: blur(60px) saturate(180%);
    -webkit-backdrop-filter: blur(60px) saturate(180%);
    box-shadow: 0 8px 40px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.2);
  }
  .glass-panel::before {
    background: linear-gradient(160deg,
      rgba(255,255,255,0.5)  0%,
      rgba(255,255,255,0.15) 30%,
      transparent            55%,
      rgba(255,255,255,0.08) 100%
    );
  }

  .glass-card {
    background: rgba(0,0,0,0.28);
    backdrop-filter: blur(30px) saturate(160%);
    -webkit-backdrop-filter: blur(30px) saturate(160%);
    box-shadow: 0 4px 20px rgba(0,0,0,0.2), inset 0 1px 0 rgba(255,255,255,0.16);
  }
  .glass-card::before {
    background: linear-gradient(145deg,
      rgba(255,255,255,0.38) 0%,
      rgba(255,255,255,0.1)  35%,
      transparent            60%,
      rgba(255,255,255,0.06) 100%
    );
  }

  .glass-pill {
    background: rgba(0,0,0,0.22);
    backdrop-filter: blur(16px) saturate(150%);
    -webkit-backdrop-filter: blur(16px) saturate(150%);
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.18);
  }
  .glass-pill::before {
    background: linear-gradient(135deg,
      rgba(255,255,255,0.4)  0%,
      rgba(255,255,255,0.08) 40%,
      transparent            70%
    );
  }

}

─────────────────────────────────────────────────────────────────────────────
RULE 2 — VIDEO BACKGROUND (two-file pattern — required for Next.js App Router)
─────────────────────────────────────────────────────────────────────────────

Create both files. The dynamic({ ssr: false }) wrapper is non-negotiable —
@react-three/fiber uses browser APIs that crash during SSR.

// components/GlassScene.tsx
'use client'
import { useRef } from 'react'
import { Canvas, useThree } from '@react-three/fiber'
import { useVideoTexture } from '@react-three/drei'
import * as THREE from 'three'

function VideoPlane({ url }: { url: string }) {
  const texture = useVideoTexture(url, { loop: true, muted: true, start: true })
  const meshRef = useRef<THREE.Mesh>(null)
  const { viewport } = useThree()
  const videoAspect = 16 / 9
  const viewportAspect = viewport.width / viewport.height
  const scaleX = viewportAspect > videoAspect ? viewport.width : viewport.height * videoAspect
  const scaleY = viewportAspect > videoAspect ? viewport.width / videoAspect : viewport.height
  return (
    <mesh ref={meshRef} scale={[scaleX, scaleY, 1]}>
      <planeGeometry args={[1, 1]} />
      <meshBasicMaterial map={texture} toneMapped={false} />
    </mesh>
  )
}

export default function GlassScene({ videoUrl }: { videoUrl: string }) {
  return (
    <Canvas style={{ position: 'absolute', inset: 0, zIndex: 0 }} camera={{ position: [0, 0, 1], fov: 50 }}>
      <VideoPlane url={videoUrl} />
    </Canvas>
  )
}

// components/GlassSceneClient.tsx
'use client'
import dynamic from 'next/dynamic'
const GlassScene = dynamic(() => import('./GlassScene'), { ssr: false })
export default function GlassSceneClient({ videoUrl }: { videoUrl: string }) {
  return <GlassScene videoUrl={videoUrl} />
}

─────────────────────────────────────────────────────────────────────────────
RULE 3 — STRICT GRAYSCALE (no brand colors anywhere in the glass UI)
─────────────────────────────────────────────────────────────────────────────

The video provides all color. The glass panels stay achromatic.
This is the constraint that makes it feel premium.

  text-white       → primary headings
  text-white/80    → card titles, secondary text
  text-white/60    → descriptions, labels
  text-white/50    → muted, timestamps, captions
  bg-white/15      → icon containers, button fills
  bg-white/10      → secondary backgrounds
  bg-white/30      → dividers
  bg-neutral-900   → page background fallback — always set this

─────────────────────────────────────────────────────────────────────────────
RULE 4 — TYPOGRAPHY (two fonts, two roles)
─────────────────────────────────────────────────────────────────────────────

// layout.tsx
import { Poppins, Source_Serif_4 } from 'next/font/google'
const poppins = Poppins({ variable: '--font-poppins', subsets: ['latin'], weight: ['400','500','600'] })
const sourceSerif4 = Source_Serif_4({ variable: '--font-source-serif', subsets: ['latin'], style: ['normal','italic'], weight: ['400','600'] })
// Apply both variables to <html>

  Poppins          → all structure: headings, UI, labels, descriptions
  Source Serif 4   → accent words in <h1> only + the footer pull quote — 2–3 words max

  Accent markup:
    <em style={{ fontFamily: 'var(--font-source-serif)', fontStyle: 'italic' }}>word</em>

  Type scale:
    H1:           text-6xl lg:text-7xl font-medium tracking-[-0.05em]
    Card titles:  text-sm font-medium
    Labels:       text-xs uppercase tracking-widest
    Descriptions: text-xs leading-relaxed

─────────────────────────────────────────────────────────────────────────────
RULE 5 — LAYOUT ARCHITECTURE
─────────────────────────────────────────────────────────────────────────────

<main className="relative min-h-screen overflow-hidden bg-neutral-900" style={{ fontFamily: 'var(--font-poppins)' }}>

  {/* z-0: full-screen video */}
  <GlassSceneClient videoUrl={VIDEO_URL} />

  {/* z-10: two-panel layout */}
  <div className="relative z-10 flex min-h-screen">

    {/* LEFT — primary: nav, h1, CTA, quote */}
    <div className="relative w-full lg:w-[52%] flex flex-col min-h-screen">
      <div className="glass-panel absolute inset-4 lg:inset-6 rounded-3xl pointer-events-none" />
      <div className="relative flex flex-col min-h-screen px-10 py-8">
        <nav>/* brand name + glass-pill nav */</nav>
        <div className="flex-1 flex flex-col items-start justify-center gap-6">
          /* tag pills, h1 with accent, tagline, CTA button */
        </div>
        <footer>/* divider + pull quote in Source Serif italic */</footer>
      </div>
    </div>

    {/* RIGHT — secondary: social bar, stats card, feature cards — desktop only */}
    <div className="hidden lg:flex w-[48%] flex-col p-6 gap-4">
      /* glass-pill top bar, glass-card community widget */
      <div className="mt-auto">
        <div className="glass-panel rounded-[2.5rem] p-6">
          <div className="grid grid-cols-2 gap-3">
            /* glass-card feature cards from CARDS brief */
          </div>
        </div>
      </div>
    </div>

  </div>
</main>

Critical: glass-panel overlay div gets pointer-events-none. Content div sits on top via relative positioning.

─────────────────────────────────────────────────────────────────────────────
RULE 6 — MICRO-INTERACTIONS (apply to every interactive element — no exceptions)
─────────────────────────────────────────────────────────────────────────────

  Buttons, cards, pills:   transition-transform hover:scale-105 active:scale-95 cursor-pointer
  Text links, icons:       transition-colors hover:text-white/80
  Icon containers:         rounded-full bg-white/15 flex items-center justify-center

The active:scale-95 is as important as hover:scale-105 — it gives tactile click feedback.

─────────────────────────────────────────────────────────────────────────────
DONE WHEN
─────────────────────────────────────────────────────────────────────────────

Before finishing, verify:
  [ ] Video loads and fills the viewport (object-cover behavior)
  [ ] Three tiers are visually distinct — blur depth is visible at a glance
  [ ] Zero color in the UI — everything is white + opacity
  [ ] Hover/active scales work on every interactive element
  [ ] Right panel is hidden on mobile (hidden lg:flex)
  [ ] ::before shimmer is visible — top-left lighter than bottom-right
  [ ] npx tsc --noEmit exits with zero errors
```

---

## What's in This Repo

```
liquid-glass-ui/
│
├── screenshot.jpeg          Visual proof — what the prompt produces
│
├── globals.css              The 3-tier glass CSS system, ready to paste
├── components/
│   ├── GlassScene.tsx       Three.js video background (WebGL, object-cover)
│   └── GlassSceneClient.tsx SSR-safe dynamic() wrapper for Next.js App Router
├── examples/
│   ├── layout.tsx           Font setup — Poppins + Source Serif 4
│   └── page.tsx             Full two-panel layout skeleton with comments
│
├── README.md                ← You are here — the prompt and brief
├── PROMPT.md                Annotated deep-dive — explains why each rule works
└── liquid-glass-ui.md       Claude Code skill — invoke with /liquid-glass-ui
```

The files in `globals.css`, `components/`, and `examples/` are the **reference implementation** — the output the prompt produces. Copy them directly if you'd rather skip the AI and wire it up yourself.

---

## Use with Claude Code

`liquid-glass-ui.md` is a Claude Code skill. Place it in `~/.claude/commands/` and invoke it from any project:

```bash
cp liquid-glass-ui.md ~/.claude/commands/liquid-glass-ui.md
```

Then type `/liquid-glass-ui` in any Claude Code session. It will parse your brief, install Three.js, write all files, and verify with `tsc`.

→ For the annotated breakdown of why each rule works: see [`PROMPT.md`](./PROMPT.md)

---

MIT License
